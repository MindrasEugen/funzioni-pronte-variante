/**
 * Arrotonda un numero per eccesso.
 * 
 * @param {number} num - Il numero da arrotondare
 * @returns {number} - Il numero arrotondato per eccesso
 */
function ceil(num) {
    return Math.ceil(num);
}

export { ceil };
