/**
 * Converte gradi in radianti.
 * 
 * @param {number} degrees - I gradi da convertire
 * @returns {number} - I radianti equivalenti
 */
function toRadians(degrees) {
    return degrees * (Math.PI / 180);
}

export { toRadians };
