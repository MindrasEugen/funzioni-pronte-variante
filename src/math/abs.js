/**
 * Restituisce il valore assoluto di un numero.
 * 
 * @param {number} num - Il numero
 * @returns {number} - Il valore assoluto
 */
function abs(num) {
    return Math.abs(num);
}

export { abs };
