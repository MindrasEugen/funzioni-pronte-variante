/**
 * Checks if a value is null.
 * Verifica se un valore è null.
 * 
 * @param {*} value - The value to check / Il valore da verificare
 * @returns {boolean} - True if the value is null / True se il valore è null
 */
function isNull(value) {
    return value === null;
}

export { isNull };
